# Models

This directory contains model metadata and conversion scripts.

## Structure
- `converters/`: Scripts to convert models from various formats

## Model Files
Large model files should not be stored in version control. Instead, provide:
- Model metadata (dimensions, version, etc.)
- Download scripts
- Checksums for verification
